name = "github-submit"
pages_build_output_dir = "."
main = "api/github-submit.js"
compatibility_date = "2026-01-01"

[vars]
# You'll set your IDs and GH keys via the Cloudflare Dashboard 
# or 'wrangler secret put'