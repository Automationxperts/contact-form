# Dependencies
node_modules

# Local environment files — never commit real credentials
.env
.env.*
!.env.example

# GitHub App private keys
*.pem

# Platform build/cache directories
.netlify
.vercel
.wrangler
dist

# Exported submissions are committed by .github/workflows/export-csv.yml.
# Only enable that workflow in the repository that stores your Discussions,
# and keep that repository private if submissions contain personal data.

# Editor / OS noise
.DS_Store
Thumbs.db
.idea
*.log

