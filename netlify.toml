[build]
  publish = "." # The directory where index.html is
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["@octokit/auth-app"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200